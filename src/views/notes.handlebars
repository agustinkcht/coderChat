<h1>NOTES</h1>
{{#each allNotes}}
    <a href="/notes/{{this.id}}" class="pt-3 ps-3">{{this.text}}</a>
{{/each}}
{{!-- loop each --}}
{{!-- this apunta a cada elemento de la iteracion sobre el array... para acceder a una key, es this.key, obviamente nos trae el value--}}